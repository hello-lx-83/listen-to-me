{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "voice-overlay",
  "description": "Minimal capabilities for the voice status overlay",
  "windows": ["voice-overlay"],
  "permissions": [
    "core:event:default",
    "core:window:default",
    "core:window:allow-hide"
  ]
}
